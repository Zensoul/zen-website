/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
  // If you want to keep Next/Image optimization on Vercel, leave this commented out.
  // If you prefer to serve raw images, uncomment the next line:
  // images: { unoptimized: true },
  // DO NOT set: output: "export"
};

export default nextConfig;
